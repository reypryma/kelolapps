import 'package:flutter/material.dart';
import 'package:nb_utils/nb_utils.dart';

class SignupStoreInfoFragment extends StatelessWidget {
  const SignupStoreInfoFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: tomato,
        height: double.infinity,
      ),
    );
  }
}
